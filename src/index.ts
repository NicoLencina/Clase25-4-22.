//declaro las variables y accedo  a los valores de HTML con la funcion document.getElementById.

let btnEnv = document.getElementById("btnEnviar");
let dato = document.getElementById("datos");
let rotulo = document.getElementById("rotulo");

//con .innerHTML nos permite leer el dato de rotulo o asignarle uno nuevo

rotulo.innerHTML = "Ingrese un dato:";
btnEnv.addEventListener("click", () => 

// Imprimo en consola el valor de la variable dato, .value evalua exactamente lo de adentro de dato.
  console.log("el dato ingresado es", dato.value;
  //
});